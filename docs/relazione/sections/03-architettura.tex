\section{Architettura del sistema}
\label{sec:architettura}

L'architettura realizzata è una pipeline distribuita nel compute continuum,
organizzata tra livelli Edge e Cloud come mostrato in
Fig.~\ref{fig:architecture}. Il sistema riproduce le osservazioni dei sensori
selezionati dal dataset storico, le elabora localmente negli Edge Site e
propaga verso il Cloud risultati progressivamente aggregati.

\begin{figure*}[t]
    \centering
    \includegraphics[width=\textwidth]{architettura.png}
    \caption{Deployment distribuito su AWS e flusso dei dati tra Simulator,
    Edge, Kafka, Cloud Worker, Global Aggregator e PostgreSQL. Amazon RDS
    appartiene alla stessa VPC ed è rappresentato separatamente nella parte
    inferiore esclusivamente per leggibilità.}
    \label{fig:architecture}
\end{figure*}

La pipeline è composta dai seguenti stadi logici.

\begin{enumerate}
    \item \textbf{Simulator:}
    Per ciascuno dei 13 Edge Site è presente un Simulator che riproduce gli
    eventi dei sensori assegnati preservandone le distanze temporali in
    event time, opportunamente accelerate. La telemetria viene pubblicata
    tramite MQTT verso il broker Mosquitto associato al sito.

    \item \textbf{Elaborazione Edge:}
    Ogni Edge Site comprende un broker MQTT e un Edge Gateway. Il gateway
    riceve e valida le misure di temperatura, umidità e pressione e le
    aggrega localmente su finestre temporali di durata configurabile. Questa
    elaborazione consente di ridurre il volume di dati inoltrato verso il
    Cloud, trasferendo statistiche aggregate anziché le singole osservazioni.

    \item \textbf{Trasporto Edge--Cloud:}
    Gli aggregati prodotti dagli Edge vengono pubblicati sul topic Kafka
    \code{edge-aggregates}. Kafka è utilizzato per disaccoppiare produttori
    e consumatori e per fornire persistenza e ordinamento dei record
    all'interno delle partizioni~\cite{kafka}. Gli Edge sono distribuiti
    staticamente tra un numero configurabile di partizioni. L'assegnamento è
    calcolato prima del deployment sulla base del numero di eventi dei replay
    shard, così da ridurre lo sbilanciamento atteso senza introdurre decisioni
    dinamiche nel data path.

    \item \textbf{Elaborazione Cloud.}
    I Cloud Worker appartengono allo stesso consumer group Kafka. Ciascuna
    partizione è assegnata a un solo worker e conserva uno stato di
    aggregazione indipendente. Lo stato è separato logicamente per partizione,
    ma risiede in memoria presso la replica assegnataria e non viene migrato:
    l'assegnamento deve pertanto restare stabile per l'intera esecuzione. I
    contributi degli Edge vengono combinati su finestre temporali di durata
    configurabile, definita come multiplo della finestra Edge. Aumentando il
    numero di worker è possibile elaborare più partizioni in parallelo, fino
    al limite imposto dal numero di partizioni del topic.

    \item \textbf{Aggregazione globale e persistenza:}
    I risultati dei Cloud Worker vengono pubblicati sul topic
    \code{cloud-partition-aggregates} e consumati dal Global Aggregator.
    Quest'ultimo combina i contributi relativi allo stesso intervallo
    temporale provenienti dalle diverse partizioni e produce un unico
    risultato globale, successivamente scritto su log oppure persistito
    in PostgreSQL.
\end{enumerate}

Il numero di Cloud Worker può essere modificato tra esecuzioni senza cambiare
la suddivisione logica del carico né il significato dei risultati prodotti.
Il numero di partizioni Kafka e il numero di repliche del microservizio Cloud Worker
sono configurabili separatamente. Le partizioni determinano la granularità con cui il
workload viene suddiviso e pongono un limite superiore al parallelismo ottenibile dal
consumer group, mentre il numero di Cloud Worker determina quanta parte di tale
parallelismo viene effettivamente sfruttata. Aumentando il numero di repliche,
le stesse partizioni vengono distribuite tra un numero maggiore di worker,
senza modificare il workload né la suddivisione logica dei dati. Questa
separazione consente di valutare la scalabilità orizzontale del livello Cloud
mantenendo invariati gli input e i risultati attesi.
