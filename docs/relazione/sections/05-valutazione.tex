\section{Valutazione sperimentale}
\label{sec:valutazione}

\subsection{Ambiente e metodologia sperimentale}

La valutazione risponde a due domande. La prima riguarda la capacità del
livello Cloud di elaborare lo stesso workload riducendo il ritardo di
produzione dei risultati all'aumentare delle repliche. La seconda riguarda il
compromesso tra latenza e completezza quando una sorgente logica viene
rallentata sul collegamento Edge--Cloud. Le prove sono eseguite sul deployment
AWS di Fig.~\ref{fig:architecture}.

Il workload definitivo comprende 2\,106\,784 osservazioni distribuite tra 13
replay shard. Il replay usa un fattore di accelerazione pari a 20\,000; tale
valore è stato scelto mediante una calibrazione preliminare in modo da
esercitare la pipeline mantenendo complete le esecuzioni utilizzate nella
campagna di scalabilità. La configurazione comune alle campagne è riassunta in
Tab.~\ref{tab:experiment-configuration}.

\begin{table}[H]
    \caption{Configurazione comune delle campagne sperimentali.}
    \label{tab:experiment-configuration}
    \centering
    \begin{tabular}{@{}lr@{}}
        \toprule
        Parametro & Valore \\
        \midrule
        Edge Site / Simulator & 13 \\
        Partizioni \code{edge-aggregates} & 6 \\
        Finestra Edge & 5 min \\
        Finestra Cloud & 15 min \\
        Fattore di accelerazione & 20\,000 \\
        Batch producer Edge & 10 \\
        Batch commit Cloud & 100 \\
        \bottomrule
    \end{tabular}
\end{table}

Per ogni run vengono conservati configurazione effettiva, log, metriche dei
container, serie temporali del consumer lag e aggregati finali PostgreSQL. La
riconciliazione verifica gli eventi offerti dai Simulator, quelli accettati e
processati dagli Edge, gli eventi presenti nei risultati globali e quelli
eventualmente scartati come late. Vengono inoltre controllati errori di
pubblicazione, duplicati, finestre incomplete, terminazione del protocollo e
lag finale.

Nella campagna di scalabilità una run è ammessa al confronto prestazionale
soltanto se il risultato globale include il 100\% degli eventi attesi, senza
contributi late o perdite a monte.

Negli esperimenti bounded, invece, i contributi late costituiscono un effetto
previsto della politica studiata e non rendono da soli non valida la run:
devono essere contabilizzati e spiegare interamente la differenza tra gli
eventi elaborati e quelli inclusi nel risultato globale. Eventuali drop nei
Simulator, negli Edge o nell'emulazione di rete rimangono invece condizioni
di invalidità della run.

La metrica principale utilizzata in entrambe le campagne è la
\emph{global emission latency}, cioè il ritardo con cui una finestra globale
viene emessa rispetto all'istante nominale associato alla sua fine nella
timeline accelerata del replay.

Indicando con $t_0$ l'istante di avvio del replay, con $e_0$ l'origine
temporale del dataset, con $f$ il fattore di accelerazione e con $w_e$ la fine
della finestra in event time, l'istante nominale associato alla finestra è

\begin{equation}
    d(w_e) = t_0 + \frac{w_e-e_0}{f}.
\end{equation}

Tale istante rappresenta il momento in cui la fine della finestra verrebbe
raggiunta da un replay ideale eseguito con il fattore di accelerazione
configurato e viene utilizzato come riferimento temporale per la misura della
latenza.

La \emph{global emission latency} è quindi calcolata come differenza tra
l'istante effettivo di emissione della finestra da parte del Global Aggregator
e $d(w_e)$. L'istante di emissione viene registrato prima del completamento
della scrittura su PostgreSQL; la metrica rappresenta pertanto il ritardo di
completamento dell'elaborazione della finestra e non il tempo necessario alla
persistenza nel database.

Per descrivere la distribuzione delle latenze vengono riportati la mediana
(p50), il 95-esimo percentile (p95) e il 99-esimo percentile (p99). La mediana
rappresenta il comportamento tipico delle finestre, mentre p95 e p99
caratterizzano la coda della distribuzione, indicando il valore entro cui
ricade rispettivamente il 95\% e il 99\% delle emissioni. Viene inoltre
riportata la durata complessiva della run, mentre consumer lag e utilizzo delle
risorse sono raccolti come metriche di controllo.

Poiché il ritmo di ingresso è determinato dal replay, un throughput medio
calcolato sul numero di eventi elaborati rifletterebbe principalmente il rate
di ingresso imposto e non la capacità massima dei Worker. La valutazione della
scalabilità si concentra pertanto sulla latenza di emissione, verificando al
tempo stesso la completezza dei risultati e l'assenza di backlog residuo.

Il lag Kafka è calcolato rispetto agli offset committati. Poiché i Cloud
Worker effettuano il commit in batch, il lag può includere temporaneamente
record già elaborati ma non ancora confermati tramite commit. Per questo
motivo viene analizzato insieme alla relativa serie temporale e al valore
finale, senza interpretarne automaticamente il massimo come backlog
applicativo effettivo.

\subsection{Scalabilità del livello Cloud}

La campagna usa un workload fisso e varia il numero di Cloud Worker tra
$W\in\{1,2,3,4,6\}$. Il topic mantiene sei partizioni in tutte le prove:
workload, partizionamento e configurazione per Worker rimangono invariati,
mentre varia il numero di esecutori tra cui Kafka distribuisce le partizioni.
Sei Worker costituiscono il limite di parallelismo utile della configurazione;
repliche ulteriori rimarrebbero senza partizioni assegnate.

Ogni run utile al calcolo ha
elaborato 2\,106\,784 eventi, prodotto 2\,710 finestre globali complete, senza
late record, duplicati o errori di protocollo, e terminato con lag nullo. La
Tab.~\ref{tab:scaling-results} riporta la media delle repliche.

\begin{table}[H]
    \caption{Risultati della campagna di scalabilità. Latenze e durata sono
    espresse in secondi.}
    \label{tab:scaling-results}
    \centering
    \begin{tabular}{@{}rrrrr@{}}
        \toprule
        $W$ & p50 & p95 & p99 & Durata \\
        \midrule
        1 & 0.57 & 1.21 & 1.40 & 125.82 \\
        2 & 0.39 & 1.08 & 1.60 & 125.53 \\
        3 & 0.39 & 0.83 & 1.16 & 125.54 \\
        4 & 0.34 & 0.83 & 1.10 & 125.45 \\
        6 & 0.35 & 1.01 & 1.58 & 126.18 \\
        \bottomrule
    \end{tabular}
\end{table}

Il miglioramento è evidente fino a quattro Worker: rispetto a W1, W4 riduce
la mediana di circa il 40\% e il p95 di circa il 32\%. Con W6 viene raggiunto
il massimo parallelismo consentito dalle sei partizioni Kafka, ma non si
osserva un ulteriore miglioramento: la mediana rimane sostanzialmente
invariata, mentre p95 e p99 risultano leggermente superiori rispetto a W4.

Il mancato miglioramento osservato con W6 può essere ricondotto anche alla
granularità del partizionamento: ciascuna partizione Kafka è assegnata a un
solo Worker del consumer group, quindi un eventuale sbilanciamento del carico
tra le sei partizioni non può essere ulteriormente suddiviso aumentando il
numero di Worker.


\subsection{Impatto delle condizioni di rete}

Le condizioni di rete sono emulate tramite Linux \code{tc-netem} nel network
namespace dei container interessati. Le regole sono selettive e non alterano
il traffico di orchestrazione o di raccolta delle metriche. Nelle
configurazioni degradate viene modificato soltanto il traffico di
\code{edge-8} verso Kafka; gli altri Edge e il collegamento Cloud--Global
rimangono invariati.

La campagna utilizza sei Cloud Worker e considera due profili di degradazione.
Nel profilo \emph{delay} vengono aggiunti 50 ms al traffico di
\code{edge-8} verso Kafka; il valore è stato scelto come perturbazione
moderata, sufficiente a introdurre disallineamento senza causare saturazione
delle code o perdite a monte.

Nel profilo \emph{bandwidth} lo stesso traffico viene limitato a 140 kbit/s,
valore prossimo al rate normalmente osservato per \code{edge-8} nelle
esecuzioni non limitate e quindi adatto a rendere il collegamento un vincolo
effettivo senza interrompere il flusso.

La politica \emph{conservative} utilizza uno skew sufficientemente ampio da
non forzare l'avanzamento della frontiera. La politica \emph{bounded} utilizza
invece uno skew massimo di 150 minuti in event time, equivalente a 450 ms
reali con fattore di accelerazione 20\,000. Se il disallineamento tra le
sorgenti supera tale limite, la frontiera può avanzare e gli aggregati
relativi a finestre già finalizzate vengono successivamente classificati
come late.

L'analisi viene suddivisa in due confronti. Il primo mantiene fissa la
politica bounded e valuta l'effetto aggiuntivo della degradazione di rete
rispetto a una configurazione di controllo senza NetEm. Il secondo confronta
le politiche conservative e bounded mantenendo invariata la condizione di
rete.

\begin{table}[H]
    \caption{Effetto della degradazione di rete con politica bounded.
    Le variazioni sono calcolate rispetto al controllo senza NetEm.}
    \label{tab:network-bounded}
    \centering
    \begin{tabular}{@{}lrrrr@{}}
        \toprule
        Condizione &
        Completezza &
        Eventi late &
        $\Delta$ completezza &
        $\Delta$ late \\
        \midrule

        Controllo
        & 89.87\%
        & 213\,388
        & --
        & -- \\

        Delay 50 ms
        & 89.12\%
        & 229\,301
        & -0.75 p.p.
        & +15\,913 \\

        Banda 140 kbit/s
        & 82.44\%
        & 370\,046
        & -7.43 p.p.
        & +156\,658 \\

        \bottomrule
    \end{tabular}
\end{table}

La configurazione di controllo mostra che lo skew bounded di 150 minuti in
event time produce contributi late anche in assenza di degradazione
artificiale della rete: la completezza è pari all'89.87\% e vengono
registrati 213\,388 contributi late. Il naturale disallineamento tra gli
Edge può quindi essere sufficiente a provocare l'avanzamento della frontiera.

Rispetto a questo riferimento, l'aggiunta di 50 ms di ritardo produce un
effetto aggiuntivo limitato: la completezza diminuisce di circa 0.75 punti
percentuali e vengono registrati 15\,913 contributi late aggiuntivi.

La limitazione della banda a 140 kbit/s produce invece un effetto più
marcato: la completezza diminuisce di circa 7.43 punti percentuali rispetto
al controllo bounded e vengono registrati 156\,658 contributi late
aggiuntivi.


\paragraph{Confronto tra politiche event-time.}

Il secondo confronto mantiene invariata la condizione di rete e valuta
l'effetto della politica di avanzamento sulla latenza e sulla completezza.

\begin{table}[H]
    \caption{Confronto tra politiche conservative e bounded a parità di
    condizione di rete. Latenze e durata sono espresse in secondi.}
    \label{tab:network-policies}
    \centering
    \resizebox{\columnwidth}{!}{%
    \begin{tabular}{@{}llrrrrrr@{}}
        \toprule
        Profilo & Politica & Compl. & Eventi late & p50 & p95 & p99 & Durata \\
        \midrule

        Delay 50 ms
        & Conservative & 100.00\% & 0
        & 1.71 & 3.87 & 4.23 & 126.31 \\

        Delay 50 ms
        & Bounded & 89.12\% & 229\,301
        & 0.57 & 0.83 & 1.16 & 126.29 \\

        \midrule

        Banda 140 kbit/s
        & Conservative & 100.00\% & 0
        & 27.01 & 37.82 & 38.61 & 162.27 \\

        Banda 140 kbit/s
        & Bounded & 82.44\% & 370\,046
        & 0.61 & 1.58 & 2.77 & 165.47 \\

        \bottomrule
    \end{tabular}%
    }
\end{table}



Con 50 ms di ritardo aggiunto al traffico di \code{edge-8}, la politica
conservative preserva il 100\% degli eventi e presenta un p95 pari a
3.87 s. La politica bounded riduce il p95 a 0.83 s, con una completezza
dell'89.12\%.

Il compromesso risulta più marcato con la limitazione di banda. La politica
conservative mantiene il 100\% degli eventi, ma il p95 raggiunge 37.82 s.
La politica bounded mantiene invece il p95 a 1.58 s, con una completezza
dell'82.44\%.

A parità di condizione di rete, la politica conservative trasferisce quindi
il rallentamento della sorgente sul tempo necessario alla finalizzazione
delle finestre. La politica bounded limita tale attesa consentendo alla
frontiera event-time di avanzare, al costo di contributi successivamente
classificati come late.



\subsection{Discussione dei risultati}

La campagna di scalabilità mostra che l'aumento del numero di Cloud Worker
riduce la latenza di emissione senza modificare workload, partizionamento o
risultati prodotti. Il beneficio è evidente fino a quattro Worker e tende a
ridursi avvicinandosi al limite di parallelismo imposto dalle sei partizioni
Kafka. Inoltre, poiché ciascuna partizione può essere assegnata a un solo
Worker, un eventuale sbilanciamento del carico tra le partizioni non può essere
eliminato semplicemente aumentando il numero di Worker.

La campagna relativa alle condizioni di rete evidenzia invece due aspetti.
In primo luogo, la configurazione bounded di controllo mostra che lo skew
scelto può produrre contributi late anche senza degradazione artificiale
della rete, a causa del naturale disallineamento nel progresso degli Edge.
Rispetto a questo riferimento, il ritardo aggiuntivo di 50 ms produce un
effetto limitato sulla completezza, mentre la limitazione di banda a
140 kbit/s determina un aumento più marcato dei contributi late.

In secondo luogo, a parità di condizione di rete emerge il compromesso tra
latenza e completezza delle due politiche event-time. La configurazione
conservative attende il progresso delle sorgenti più arretrate e preserva la
completezza, trasferendo il disallineamento sulla latenza di finalizzazione.
La configurazione bounded limita invece tale attesa consentendo alla frontiera
di avanzare, riducendo la latenza al costo di contributi successivamente
classificati come late.